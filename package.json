{
  "name": "crypto-pay-ecosystem-contract",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-solhint": "^2.0.0",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.4.1",
    "hardhat": "^2.4.3",
    "web3": "^1.7.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.4.1",
    "@openzeppelin/test-helpers": "^0.5.15",
    "dotenv": "^10.0.0"
  },
  "scripts": {
    "deploy": "npx hardhat run scripts/deploy.js --network local",
    "deploy:cronos": "npx hardhat run scripts/deploy.js --network cronos",
    "lint": "npx hardhat check",
    "test": "npx hardhat test",
    "console": "npx hardhat console --network local",
    "console:cronos": "npx hardhat console --network cronos"
  }
}
